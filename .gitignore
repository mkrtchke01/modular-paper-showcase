# Сырьё от заказчика (тяжёлые 8K-рендеры, исходные логотипы и т.п.) — в репозиторий не нужны.
# Все реально используемые картинки уже лежат в assets/ (оптимизированы для веба).
Исходники от заказчика/

# IDE / OS
.idea/
.DS_Store
Thumbs.db
desktop.ini
